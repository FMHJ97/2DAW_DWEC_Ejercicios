export default function(a,b) {
    console.log(a + " + " + b + " = " + (a+b));
}

// Si el ponemos un nombre a la función (sumar),
// debemos llamarla de esa manera obligatoriamente.

// export default function sumar(a,b) {
//     console.log(a + " + " + b + " = " + (a+b));
// }